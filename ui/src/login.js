import "./reset.css";
import "./login.css";

/*
document.forms[0].addEventListener("submit", function onSubmit(e) {
    const input = e.target.elements[0];
    const { name, value } = input;
    e.preventDefault();
    return false;
});
*/